import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_para_pruebas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
